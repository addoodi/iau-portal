# Portainer Environment Variables
# Copy these values to Portainer Stack -> Environment variables section

# ============================================
# NEW: Simplified Configuration!
# ============================================
# The application now uses nginx reverse proxy
# Only ONE port needed - nginx proxies /api to backend automatically
# No more CORS issues or port configuration complexity!

# Frontend port (this is the ONLY port you'll access)
# Default: 3000
FRONTEND_PORT=3000

# To use a different port (e.g., 8098):
# FRONTEND_PORT=8098

# Then access everything at: http://your-server-ip:8098
# - Frontend: http://your-server-ip:8098
# - API: http://your-server-ip:8098/api/...

# ============================================
# Data Persistence (Optional):
# ============================================
# By default, data is stored in the stack directory (./backend/data)
# For permanent storage that survives stack deletion, set these:

# DATA_PATH=/opt/iau-portal/data
# TEMPLATES_PATH=/opt/iau-portal/templates

# Before setting these, create the directories on your server:
# mkdir -p /opt/iau-portal/data
# mkdir -p /opt/iau-portal/templates

# ============================================
# OLD (No longer needed):
# ============================================
# BACKEND_PORT - removed (backend only accessible through nginx proxy)
# VITE_API_URL - removed (uses relative path /api)
