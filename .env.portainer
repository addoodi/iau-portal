# Portainer Environment Variables
# Copy this file content to Portainer Stack environment variables

# Image version tag (optional - use for versioning)
IMAGE_TAG=latest

# Cache buster - Change this value to force rebuild
# Use current timestamp: date +%s (Linux) or Get-Date -UFormat %s (PowerShell)
# Example values: 1, 2, 3, or timestamp like 1704067200
CACHEBUST=1

# Frontend port (default: 3000)
FRONTEND_PORT=3000
